# Color-Meaning-Checker# 🎨 Color Meaning Checker (Java)

## 📌 Overview
The **Color Meaning Checker** is a simple and interactive Java console project built as part of an internship.  
It allows users to input a color name and get its symbolic or emotional meaning.  

This project demonstrates the use of **HashMap**, **loops**, **user input handling**, and **string manipulation** in Java.

---

## 🚀 Features
- 🔍 Input any color name to see its meaning  
- 🧠 Uses `HashMap` for storing color-meaning pairs  
- ⚡ Fast and efficient lookup  
- 🎯 Simple console-based interface  
- 💬 User-friendly output with emojis  

---

## 🧠 Tech Stack
- **Language:** Java  
- **IDE Used:** IntelliJ IDEA / Eclipse / VS Code  
- **Concepts Used:** HashMap, Scanner, Loops, Conditional Statements  

---

## ⚙️ How It Works
1. User runs the program in the terminal.  
2. Enters a color name (e.g., `red`, `blue`, `green`).  
3. The system checks the meaning from a predefined list.  
4. Displays the meaning with an emoji.  
5. User can type `exit` to quit the program.  

---

## 💻 Example Output
